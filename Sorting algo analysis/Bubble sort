%Bubble sort:
function [ c ] = bubble( A,i )
 c=0;
 for j=1:1:i
        for k=1:i-j-1
            c=c+1;
            if A(k+1)<A(k)
                temp = A(k);
                A(k)=A(k+1);
                A(k+1)=temp;
            end
        end
    end
 
end
 

